<% provide(:title, 'The Popper') %>
<div>
  <h1>The Popper</h1>
  <h2>Find some Kernels and crunch away. Napkins not included.</h2>
</div>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>